<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="@android:style/Theme.DeviceDefault.NoActionBar">
        <item name="android:statusBarColor">@color/gray_custom</item>
        <item name="android:windowBackground">@color/gray_custom</item>
        <item name="android:navigationBarColor">@color/gray_custom</item>
    </style>

</resources>
